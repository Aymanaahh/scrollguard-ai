Load the `extension/` folder as an unpacked extension in Chrome and start the local Uvicorn backend before using either the content-script banner or the popup's Scan This Page feature; the popup displays an error if the backend connection fails.