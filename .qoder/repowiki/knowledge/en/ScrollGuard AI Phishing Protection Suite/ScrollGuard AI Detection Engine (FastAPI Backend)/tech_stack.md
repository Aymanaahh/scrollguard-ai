FastAPI + Pydantic for request/response modeling; OpenAI Python SDK targeting Alibaba DashScope's OpenAI-compatible endpoint (`https://dashscope-intl.aliyuncs.com/compatible-mode/v1`) with models `qwen3.5-plus-2026-02-15` (service) and `qwen3.7-plus` (connectivity test); python-dotenv for secret management; scikit-learn (`classification_report`, `confusion_matrix`) in the evaluation harness; `requests` for the evaluation/test scripts.